# @charlie-labs/system-cli

This is a placeholder CLI for the `charlie-system` Bun workspace. It exists
only to prove the workspace CLI/package wiring.

Delete this placeholder CLI, including this README, as soon as the real CLI
code is added to this repository. It is temporary scaffolding, not the starting
point for the production CLI.

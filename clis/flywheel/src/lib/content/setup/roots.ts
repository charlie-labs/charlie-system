import path from 'node:path';

export const CUSTOMER_SCAFFOLD_ROOT = path.resolve(
  import.meta.dir,
  '../../../../scaffolds/customer'
);

export const SOURCE_REPOSITORY_SCAFFOLD_ROOT = path.resolve(
  import.meta.dir,
  '../../../../scaffolds/source-repo'
);

description: UPD-010 overlapping hunks should error
expect: error
before:
  src/overlap.ts: |-
    const items = [1, 2, 3, 4, 5];

    function sum(arr: number[]) {
      return arr.reduce((a, b) => a + b, 0);
    }
patch: |-
  *** Begin Patch
  *** Update File: src/overlap.ts
  @@ first hunk
  -function sum(arr: number[]) {
  -  return arr.reduce((a, b) => a + b, 0);
  +function sum(arr: number[]) {
  +  return arr.reduce((a, b) => a + b, 0);
  }
  @@ second hunk
  -  return arr.reduce((a, b) => a + b, 0);
  +  return arr.reduce((a, b) => a + b, 0) / arr.length;
  *** End Patch

mutation CommentCreate($input: CommentCreateInput!) {
  commentCreate(input: $input) {
    success
    lastSyncId
    comment {
      id
      body
      createdAt
      updatedAt
      user {
        id
        name
        displayName
        avatarUrl
      }
    }
  }
}

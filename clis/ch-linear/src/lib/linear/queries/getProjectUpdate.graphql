query GetProjectUpdate($id: String!) {
  projectUpdate(id: $id) {
    id
    createdAt
    updatedAt
    body
    url
    health
    user {
      ...UserLite
    }
    project {
      id
      name
      slugId
    }
    diffMarkdown
  }
}

# import "./initiativeFields.graphql"

mutation CreateInitiative($input: InitiativeCreateInput!) {
  initiativeCreate(input: $input) {
    success
    initiative {
      ...InitiativeFields
    }
  }
}

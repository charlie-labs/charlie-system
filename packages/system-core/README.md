# @charlie-labs/system-core

This is a placeholder package for the `charlie-system` Bun workspace. It exists
only to prove that the placeholder CLI can consume a workspace package.

Delete this placeholder package, including this README, as soon as the real
package code is added to this repository. It is temporary scaffolding, not a
production API.
